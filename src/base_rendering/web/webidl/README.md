# Generating Pascal units from webidl files

Script `mk_castlejobweb.sh` takes all webidl files from `parts/*.webidl` and processes them using `webidl2pas`. Result is the unit `castleinternaljobweb.pas` in parent directory.
