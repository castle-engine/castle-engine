{ Disable warnings about deprecated stuff and
  notes "Values in enumeration types have to be ascending"
  ("warn 3031 off" doesn't do the trick on clean rebuild with FPC 3.2.2). }
{$warnings off}
{$notes off}
