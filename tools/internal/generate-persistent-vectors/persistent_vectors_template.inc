{ -*- buffer-read-only: t -*-

  Copyright 2018-2022 Michalis Kamburelis.

  This file is part of "Castle Game Engine".

  "Castle Game Engine" is free software; see the file COPYING.txt,
  included in this distribution, for details about the copyright.

  "Castle Game Engine" is distributed in the hope that it will be useful,
  but WITHOUT ANY WARRANTY; without even the implied warranty of
  MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.

  ----------------------------------------------------------------------------
}

{ Automatically generated wrapper to make vectors/colors persistent.

  Do not edit this file manually!
  Instead
  - edit tools/internal/generate-persistent-vectors/persistent_vectors_input.txt
  - and run generate-persistent-vectors in that directory.
}

{$ifdef read_interface_class}
strict private
  F${PROPERTY_NAME}Persistent: ${PERSISTENT_PROPERTY_CLASS};
  function Get${PROPERTY_NAME}ForPersistent: ${PROPERTY_TYPE};
  procedure Set${PROPERTY_NAME}ForPersistent(const AValue: ${PROPERTY_TYPE});
published
  { @link(${PROPERTY_NAME}) that can be visually edited in
    Castle Game Engine Editor, Lazarus and Delphi.
    Normal user code does not need to deal with this,
    instead read or write @link(${PROPERTY_NAME}) directly.

    @seealso ${PROPERTY_NAME} }
  property ${PROPERTY_NAME}Persistent: ${PERSISTENT_PROPERTY_CLASS} read F${PROPERTY_NAME}Persistent ${PROPERTY_STORED};
{$endif read_interface_class}

{$ifdef read_implementation_methods}
function ${CLASS_NAME}.Get${PROPERTY_NAME}ForPersistent: ${PROPERTY_TYPE};
begin
  Result := ${PROPERTY_NAME};
end;

procedure ${CLASS_NAME}.Set${PROPERTY_NAME}ForPersistent(const AValue: ${PROPERTY_TYPE});
begin
  ${PROPERTY_NAME} := AValue;
end;
{$endif read_implementation_methods}

{$ifdef read_implementation_constructor}
  F${PROPERTY_NAME}Persistent := ${PERSISTENT_PROPERTY_CLASS}.Create(nil);
  { Note that some classes (like TCastleVector3Persistent) call SetSubComponent(true)
    on themselves, for backward compatibility. But we should not depend on it,
    some classes (like TFloatRectanglePersisten) may not call it.
    The responsibility of calling "SetSubComponent(true)" should be on the owner
    in general (this is more functional, and more standard too). }
  F${PROPERTY_NAME}Persistent.SetSubComponent(true);
  F${PROPERTY_NAME}Persistent.InternalGetValue := {$ifdef FPC}@{$endif}Get${PROPERTY_NAME}ForPersistent;
  F${PROPERTY_NAME}Persistent.InternalSetValue := {$ifdef FPC}@{$endif}Set${PROPERTY_NAME}ForPersistent;
  F${PROPERTY_NAME}Persistent.InternalDefaultValue := ${PROPERTY_NAME}; // current value is default
{$endif read_implementation_constructor}

{$ifdef read_implementation_destructor}
  FreeAndNil(F${PROPERTY_NAME}Persistent);
{$endif read_implementation_destructor}
