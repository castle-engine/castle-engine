# Post-process some icons using CGE image processing

Here we express some image post-processing operations using the Castle Game Engine API.

Right now, this is used to process a subset of icons from `../exported/`

- to `exported_dark_inactive`
- to `exported_dark_active`
- to `exported_dark_disabled`
