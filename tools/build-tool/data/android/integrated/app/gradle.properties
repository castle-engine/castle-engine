# Use AndroidX instead of the support library, following Google advises.
# See https://developer.android.com/jetpack/androidx/migrate
#
# And we need the support library (later: AndroidX)
# for requestPermissions implementation

android.useAndroidX=true
android.enableJetifier=true
