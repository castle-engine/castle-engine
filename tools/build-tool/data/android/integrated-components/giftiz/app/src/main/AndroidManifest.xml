<?xml version="1.0" encoding="utf-8"?>
<!-- This manifest will be merged with main integrated/AndroidManifest.xml
     by the build tool. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity android:name="com.purplebrain.giftiz.sdk.GiftizWebViewActivity" />
        <meta-data android:name="GIFTIZ_PARTNER_KEY" android:value="${ANDROID.GIFTIZ.PARTNER_KEY}" />
        <!-- optional, only for debugging -->
        <!-- <meta-data android:name="SHOW_GIFTIZ_LOG" android:value="true" /> -->
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
</manifest>
